% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge-headers.R
\name{merge_headers}
\alias{merge_headers}
\title{Merge headers in flextables created from gtsummary objects}
\usage{
merge_headers(x)
}
\arguments{
\item{x}{A \code{flextable} object, typically produced by
\code{\link[gtsummary:as_flex_table]{gtsummary::as_flex_table()}}.}
}
\value{
The modified \code{flextable} object.
}
\description{
Vertically extends visible header cells through contiguous blank padding
cells immediately above them when the resulting merged area is rectangular.
Ordinary column labels and spanning headers are treated equally.
}
\details{
This function is intended primarily for \code{flextable} objects produced by
\code{\link[gtsummary:as_flex_table]{gtsummary::as_flex_table()}}, which uses \code{" "} (a single space) for missing
spanning-header cells. A cell containing exactly \code{" "} is therefore treated
as structural padding. Call this function immediately after conversion and
before applying additional header styling.

Existing horizontal blank spans are split only where required for a
vertical merge. Header content is never crossed, and cells that are already
merged vertically are left unchanged.
}
\examples{
\dontshow{if ((identical(Sys.getenv("NOT_CRAN"), "true") || identical(Sys.getenv("IN_PKGDOWN"), "true")) && requireNamespace("gtsummary", quietly = TRUE)) withAutoprint(\{ # examplesIf}
gtsummary::tbl_summary(
  gtsummary::trial,
  by = "trt",
  include = c("age", "marker")
) |>
  gtsummary::modify_spanning_header(
    gtsummary::all_stat_cols() ~ "**Treatment Received**"
  ) |>
  gtsummary::as_flex_table() |>
  merge_headers()
\dontshow{\}) # examplesIf}
}
